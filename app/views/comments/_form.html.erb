<%= form_for [@job, Comment.new] do |f| %>
    <div class="field">
        <%= f.label :name %> <br>
        <%= f.text_field :name %> <br>
    </div>

    <div class="field">
        <%= f.label :body %> <br>
        <%= f.text_field :body %> <br>
    </div>

    <div class="actions">
        <%= f.submit "Enviar" %>
    </div>
<% end %>